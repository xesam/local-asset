org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.configuration-cache=true
android.useAndroidX=true
kotlin.code.style=official

# --- Publishing coordinates (shared by :localasset-core and :localasset-webview) ---
GROUP=io.github.xesam
VERSION_NAME=0.0.1

# --- Release credentials (optional; absent => build/test still pass, only publish/sign fail) ---
# Maven Central Portal:        mavenCentralUsername / mavenCentralPassword
# In-memory GPG signing:        signingKeyId / signingKey / signingPassword
